Verify closed fd
	cat "test" | read &>-

Verify shell behavior when using unicode

Verify correct implementation of local/global variables

		> b=32			| set b=42 as local
		> export b		| export b local var to global env
		> b=52			| modify b in local and global
		> unset b		| delete b from local and global

Verify correct execution of functions which use env vars when those vars are unset/mis-set

	$HOME
		env -i shell
		cd builtin
		tilde in lexer
		set prompt
	$PWD, $OLDPWD, $CDPATH
		cd builtin
	$HISTFILE
		history builtin when using file options without specifying filepath
	$CONFIG
		config import at shell init
	$TERM
		nowhere
	$SHLVL
		nowhere

Verify parsing of builtins including options

		> export PATH=/local/bin:$PATH

Verify correct execution of functions when using env [-i]

Verify leaks

Verify malloc (github/dlaurent42/secure_malloc)

Verify limits of str, arr, _t * by modifying constants in .h

Verify shell exec without variables used in env_search

Verify forbidden functions